# utils/darkweb.py

def search_darkweb(brand):
    # Simulated logic; replace with real API (e.g., IntelligenceX, dark feed, etc.)
    leaked_keywords = ["password", "api_key", "leaked"]
    if brand.lower() in ["examplecorp", "testbrand"]:
        return f"⚠️ Pastebin leak mentioning `{brand}` with suspicious terms."
    return "No relevant Pastebin or dark web results."
